This demo includes:
1. The Simulation of Zeta Protocol in Full Connected Topology
    if you want to view this demo. please run the command below
    ns full-connected.tcl
2. The Simulation of Zeta Protocol in Star Topology
    if you want to view this demo. Please run the command below
    ns star.tcl
3. The Simulation of Zeta Protocol in Transit-stub topology
    if you want to view this demo. Please run the command below
    ns real-internet.tcl

*****************************
*  Description of Each File *
*****************************
---------------------
--full-connected.tcl--
---------------------
 is the demo in full connected topology

---------------------
--star.tcl         --
---------------------
 is the demo in star topology

---------------------
--real-internet.tcl--
---------------------
 is the demo in transit-stub topology

---------------------
--ts200.tcl       --
---------------------
 the topology generated by gt-itm, will be used in real-internet.tcl.
 if you want to view the topology, please run:
 ns nsam_show.tcl

---------------------
--ana.m       --
---------------------
 a Matlab/Octave function to process output data. It can get the average dowloading 
 speed of each node
 The first paramenter is the total size of data
 The second parameter is the path of output result file.

---------------------
--GetBSLoad.m       --
---------------------
 It can get the total size of data sent and received by Boot Server and other nodes
 The first parameter is the path of output trace file
 The second parameter is the set of Boot Server.


********************************************
*  Analyze data using Matlab or Octave     *
********************************************
the files in mfiles directory is for processing data. if you want to
use it, you must install MATLAB or Octave first. In Ubuntu, run the 
following command to install octave:

sudo apt-get install octave

An exampele, 
if you want to analyze the performance of Zeta protocol in full-
connecte topology. please run:

ns full-connected.tcl > res/full-connected.txt
cp out.tr res/full-connected.tr
cd mfiles
octave
ana(300000, '../res/full-connected.txt')
GetLoad('../res/full-connected.tr', 0)


then the result will be displayed
 
